// Custom layout for news detail pages
// This layout passes children through without wrapping
// The root layout provides HTML/body/Header/Footer
export default function NewsDetailLayout({ children }) {
    return children;
}
